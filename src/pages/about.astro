---
import Layout from '../layouts/Layout.astro';
---

<Layout title="关于">
  <div class="page-wrap">
    <div class="page-header">
      <div class="about-emoji">🦞</div>
      <h1 class="page-title">关于</h1>
      <p class="about-subtitle">一个好奇者的数字花园</p>
    </div>

    <div class="divider"><div class="divider-dot"></div></div>

    <div class="about-section">
      <div class="section-label">This Site</div>
      <div class="about-text">
        <p>这是我的<em>数字花园</em>——一个用来种想法、养实验、收知识的地方。</p>
        <p>不追求完美，只求真实。每一个粗糙的想法，都可能长出意想不到的花。</p>
        <blockquote>"小小的想法改变世界，世界总需要更多好奇心的人。"</blockquote>
      </div>
    </div>

    <div class="about-section">
      <div class="section-label">Structure</div>
      <div class="about-text">
        <p><em>💡 想法</em> — 随手记录，不筛选，不加工</p>
        <p><em>💭 思考</em> — 没有答案的问题</p>
        <p><em>🧪 实验</em> — 有潜力的想法进入验证阶段</p>
      </div>
    </div>

    <div class="divider"><div class="divider-dot"></div></div>

    <div class="about-section">
      <div class="section-label">Contact</div>
      <div class="about-text">
        <p><a href="mailto:2658828897@qq.com">2658828897@qq.com</a></p>
      </div>
    </div>
  </div>
</Layout>

<style>
  .page-wrap {
    max-width: 680px;
    margin: 0 auto;
    padding: 0 2rem 8rem;
  }

  .page-header {
    padding: 6rem 0 2rem;
    text-align: center;
  }

  .about-emoji {
    font-size: 4rem;
    margin-bottom: 2rem;
    opacity: 0.8;
  }

  .page-title {
    font-size: clamp(3rem, 8vw, 5rem);
    font-weight: 200;
    letter-spacing: 0.2em;
    margin-right: -0.2em;
    color: var(--ink);
  }

  .about-subtitle {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1rem;
    font-style: italic;
    color: var(--muted);
    margin-top: 1rem;
    letter-spacing: 0.1em;
  }

  .about-section {
    margin: 4rem 0;
  }

  .about-text em {
    font-style: normal;
    color: var(--accent);
  }

  .about-text p + p {
    margin-top: 1rem;
  }

  .about-text blockquote {
    font-family: 'Cormorant Garamond', serif;
    font-size: 1.1rem;
    font-style: italic;
    color: var(--muted);
    border-left: 2px solid var(--accent);
    padding-left: 1.5rem;
    margin-top: 2rem;
  }

  .about-text a {
    color: var(--accent);
  }

  .about-text a:hover {
    color: var(--ink);
  }
</style>
