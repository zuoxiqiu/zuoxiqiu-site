---
import Layout from '../layouts/Layout.astro';
---

<Layout title="关于" description="关于昨夕秋这个数字花园，以及它为什么会这样存在。">
  <section class="page-wrap page-shell about-page">
    <div class="page-header reveal">
      <div class="page-header-copy">
        <p class="page-kicker">About this garden</p>
        <h1 class="page-title">关于</h1>
        <p class="page-desc">
          这是一个把灵感、判断和实验放在同一个空间里持续整理的地方。它不急着把所有东西都写成结论，更在意中间地带有没有被认真留下。
        </p>
      </div>

      <aside class="page-panel">
        <p class="page-panel-label">Site note</p>
        <p class="page-panel-note">
          真正有生命力的网站，不只是好看，而是有自己的呼吸方式。这里的内容会继续变，所以页面也被设计成能够承受“变化”的样子。
        </p>
      </aside>
    </div>

    <div class="about-grid reveal">
      <article class="about-card">
        <p class="panel-kicker">What lives here</p>
        <h2>想法、问题、实验</h2>
        <p>
          不同阶段的内容被放进不同轨道里：灵感先出现，思考慢慢沉淀，值得动手的部分再进入实验。
        </p>
      </article>

      <article class="about-card">
        <p class="panel-kicker">Why keep it public</p>
        <h2>把过程留下来</h2>
        <p>
          有些东西如果只保留最终答案，会显得干净，但也会失去生成它的纹理。这个站更像公开的工作台，而不是展厅。
        </p>
      </article>

      <article class="about-card">
        <p class="panel-kicker">How it grows</p>
        <h2>持续更新，而不是一次完成</h2>
        <p>
          这里允许粗糙，允许回看，也允许撤回。重要的不是一篇内容有多完美，而是这片场域是否持续在生长。
        </p>
      </article>
    </div>

    <section class="about-quote reveal">
      <p class="panel-kicker">A line to keep</p>
      <blockquote>把模糊的东西先留下来，本身就是一种诚实的创造方式。</blockquote>
    </section>

    <section class="about-contact reveal">
      <div class="about-contact-copy">
        <p class="page-kicker">Contact</p>
        <h2>如果你也对这些内容方向感兴趣，可以直接联系我。</h2>
      </div>

      <div class="about-contact-actions">
        <a class="button button-primary" href="mailto:xavier_xq@126.com">xavier_xq@126.com</a>
        <a class="button button-secondary" href="/">回到首页</a>
      </div>
    </section>
  </section>
</Layout>

<style>
  .about-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));
    gap: 1rem;
    margin-top: 2rem;
  }

  .about-card,
  .about-quote,
  .about-contact {
    position: relative;
    overflow: hidden;
    border: 1px solid var(--line);
    border-radius: var(--radius-xl);
    background:
      radial-gradient(circle at 0% 0%, rgba(255, 255, 255, 0.82), transparent 30%),
      var(--surface);
    box-shadow: var(--shadow-soft);
  }

  .about-card {
    display: grid;
    gap: 0.9rem;
    padding: 1.5rem;
  }

  .about-card h2 {
    font-size: clamp(1.9rem, 3vw, 2.5rem);
    line-height: 1.02;
  }

  .about-card p:last-child {
    color: var(--muted);
  }

  .about-quote {
    display: grid;
    gap: 0.8rem;
    margin-top: 2rem;
    padding: 2rem;
  }

  .about-quote blockquote {
    max-width: 16ch;
    font-size: clamp(2.2rem, 4.4vw, 3.8rem);
    line-height: 1.02;
  }

  .about-contact {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 1.4rem;
    align-items: center;
    margin-top: 2rem;
    padding: 2rem;
  }

  .about-contact-copy {
    display: grid;
    gap: 0.8rem;
  }

  .about-contact h2 {
    font-size: clamp(2.1rem, 4.5vw, 3.6rem);
    line-height: 0.98;
    text-wrap: balance;
  }

  .about-contact-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  @media (max-width: 980px) {
    .about-grid,
    .about-contact {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 720px) {
    .about-card,
    .about-quote,
    .about-contact {
      border-radius: 24px;
    }

    .about-contact-actions {
      display: grid;
    }
  }
</style>
